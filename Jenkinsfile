#!/usr/bin/env groovy

// Load the local library that is stored under vars/
// Identifier isn't really used but we need to set it
library(identifier: 'local-library@local', retriever: legacySCM(scm))

jenkinsPipeline()
